name: ASRS RAG CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Testing Dependencies
        # Added langchain and langchain-community to fix the Ragas dependency error
        run: |
          python -m pip install --upgrade pip
          pip install pytest requests ragas datasets langchain langchain-community

      - name: Build and Start Containers
        run: |
          docker compose up -d --build backend

      - name: Run Automated Tests
        run: |
          pytest integration_tests/test_rag.py

